import React from 'react';
import PropTypes from 'prop-types';

const Skillcard = (props) => {
	return (
		<article>
			my job is to showcase the skills of aditya and match them color wise.
		</article>
	);
};

Skillcard.propTypes = {};

export default Skillcard;
