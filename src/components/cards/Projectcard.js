import React from 'react';
import PropTypes from 'prop-types';

const Projectcard = (props) => {
	return (
		<article>
			my job is to show you the projects done by aditya as cards
		</article>
	);
};

Projectcard.propTypes = {};

export default Projectcard;
